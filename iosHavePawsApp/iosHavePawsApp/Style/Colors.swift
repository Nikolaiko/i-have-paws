import SwiftUI
import shared

let transparentUIColor = UIColor.white.withAlphaComponent(0.0)
let transparentColor = Color.white.opacity(0.0)

let mainBackgroundColor = Color.init(hex: UInt(ColorsKt.mainBackgroundColor))
let mainPurpleColor = Color.init(hex: UInt(ColorsKt.mainPurpleColor))
let mainRedButtonColor = Color.red
let mainDisabledButtonColor = Color.gray

let tabTiteBackgroundColor = Color.init(hex: UInt(ColorsKt.tabTitleBackgroundColor))
let tabTitleTextColor = Color.init(hex: UInt(ColorsKt.mainTextColor))

let blueLight20 = Color.init(hex: UInt(ColorsKt.blueLight20))
let blueLight30 = Color.init(hex: UInt(ColorsKt.blueLight30))
let bluePrimary = Color.init(hex: UInt(ColorsKt.bluePrimary))
